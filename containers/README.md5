```bash
docker build -t plate-api:v2.1 ./plate-api
docker save -o plate-api-v2.1.tar plate-api:v2.1
docker load -i plate-api-v2.1.tar
```


```bash
docker compose down

docker images

docker build -t httpserver:v1.0 ./httpserver

docker save httpserver.tar httpserver:v1.0

docker compose up -d
```
